import Layout from './components/Layout';
import {hot} from "react-hot-loader";

function App() {
  return (
    <div>
      <Layout/>
    </div>
  );
}

// export default hot(module)(App);
export default App;
